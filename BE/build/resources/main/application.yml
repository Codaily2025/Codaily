spring:
  # datasource:
  #   url: jdbc:postgresql://localhost:5432/{????}
  #   username: {PostgreSQL ???}
  #   password: {????}
  #   driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

server:
    port: ${PORT:8081}

